<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.sinosoft.platform.core.domain.mapper.SqlAdapterMapper">
    <select id="excuteQuery" parameterType="sqlAdapter" resultType = "java.util.Map">
       ${sql}
    </select>
    <select id="excuteQueryCount" parameterType="sqlAdapter" resultType="java.math.BigDecimal">
       ${sql}
    </select>
    <select id="excuteParamPOJO" parameterType="sqlAdapter" resultType="com.sinosoft.platform.core.domain.pojo.ParamPOJO">
      ${sql}
    </select>
    <update id="excuteUpdate" parameterType="sqlAdapter">
       ${sql}
    </update>
    <insert id="excuteInsert" parameterType="sqlAdapter">
       ${sql}
    </insert>
    <delete id="excuteDelete" parameterType="sqlAdapter">
       ${sql}
    </delete>
    
</mapper>  